{
  "name": "livraison-project",
  "version": "1.0.0",
  "description": "Livraison Express - Full Stack Delivery App",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd livraison-frontend && npm start",
    "build": "cd livraison-frontend && npm run build",
    "android": "cd livraison-frontend && npx cap run android",
    "install-all": "npm install && cd backend && npm install && cd ../livraison-frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
