// src/config/apiConfig.js
const API_CONFIG = {
  // في التطبيق المحمول، استخدم عنوان IP الحقيقي
  BASE_URL: "http://192.168.1.100:8080/api",
  
  // أو إذا كان الخادم لا يعمل، استخدم وضع التطوير
  MOCK_MODE: false
};

export default API_CONFIG;
